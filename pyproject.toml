[project]
name = "databasus-ansible-collection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ansible>=14.1.0",
    "ansible-doc-extractor>=0.1.11",
    "argparse>=1.4.0",
]

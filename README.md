## Databasus Ansible Collection

This project provides an autogenerated Ansible Collection for Databasus, based on the platform OpenAPI specification.

Its main goal is to let infrastructure and platform teams manage Databasus resources through Ansible in a consistent and repeatable way, including authentication, databases, backups, storage, users, notifications, and other API-backed operations.

The collection source is generated from the API spec to keep modules aligned with Databasus endpoints and reduce manual maintenance.

For more information about the platform, visit [Databasus](https://databasus.com/).

## Disclaimer

This project is currently under active development.

It is not recommended for production use at this stage.

Use it at your own risk.

## Development

Use the generator script to rebuild the Ansible collection from the OpenAPI/Swagger spec.

### Generate Collection

Run the command below from the repository root:

```bash
uv run python3 scripts/generate_collection.py --spec openapi.json --output ansible_collections/zeqk/databasus
```

What this command does:

- Reads the API spec from `openapi.json`.
- Regenerates `galaxy.yml`, module files, and the collection README.
- Writes output into `zeqk/databasus`.

### Generate docs

```bash
uv run ansible-doc-extractor \
    docs \
    $PWD/ansible_collections/zeqk/databasus/plugins/modules/*.py \
    --markdown \
    --template module_doc.md.j2
```

### Notes

- Re-run this command every time `openapi.json` changes.
- Generated files are overwritten to keep the collection in sync with the API spec.
